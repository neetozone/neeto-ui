import React from "react";nnimport classNames from "classnames";nimport { Resizable } from "react-resizable";nnimport CellContent from "./CellContent";nnconst HeaderCell = props => {n  const { n    onResize, n    width, n    minWidth = 80,n    onResizeStop, n    onResizeStart,n    "data-column-index": columnIndex,n    "data-column-key": columnKey,n    className, n    ...restProps n  } = props;nn  if (!width) {n    return (n      <CellContentn        {...restProps}n        className={classNames(className, "drag-handler")}n      />n    );n  }nn  return (n    <Resizablen      width={width}n      height={0}n      minConstraints={[minWidth, 0]}n      maxConstraints={[width * 3, 0]} // Allow up to 3x the current widthn      onResize={onResize}n      onResizeStop={onResizeStop}n      onResizeStart={onResizeStart}n      draggableOpts={{ enableUserSelectHack: false }}n      handle={n        <spann          className="react-resizable-handle"n          onClick={e => e.stopPropagation()}n          data-column-index={columnIndex}n          data-column-key={columnKey}n          title="Drag to resize column"n        >n          <span className="neeto-ui-table-react-resizable-handle__inner" />n        </span>n      }n    >n      <CellContentn        {...restProps}n        className={classNames(className, "drag-handler")}n      />n    </Resizable>n  );n};nnexport default React.memo(HeaderCell);